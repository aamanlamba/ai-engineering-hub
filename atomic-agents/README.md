# Atomic Agents

This project explores the atomic-agents library to build AI agents

## Installation and setup

**Get API Keys**:

- 

**Install Dependencies**:
   Ensure you have Python 3.11 or later installed.

```bash
   pip install atomic_agents
```
